## 1. Crear el Componente Reutilizable SectionHeader

- [x] 1.1 Crear `src/components/ui/section-header.tsx` soportando props: `eyebrow`, `title`, `description`, `align` ("left" | "center" | "split"), `tone` ("light" | "dark"), y animaciones con `<Reveal>`.
- [x] 1.2 Añadir utilidades tipográficas complementarias en `src/app/globals.css` si se requieren para el layout de 2 columnas o variantes de tono.

## 2. Refactorizar las Secciones de la Landing Page

- [x] 2.1 Actualizar el encabezado de `Projects.tsx` ("PROYECTOS DESTACADOS").
- [x] 2.2 Actualizar el encabezado de `Services.tsx` ("SERVICIOS").
- [x] 2.3 Actualizar los encabezados de las páginas internas (`/estudio`, `/servicios`, `/contacto`).

## 3. Verificación y Validaciones

- [x] 3.1 Probar la coherencia visual en resoluciones móvil, tablet y escritorio.
- [x] 3.2 Verificar que `pnpm build` compile correctamente sin errores de TypeScript ni de linter.
